'use strict';const elementToggleFunc=function(elem){elem.classList.toggle("active");}
const sidebar=document.querySelector("[data-sidebar]");const sidebarBtn=document.querySelector("[data-sidebar-btn]");if(sidebar&&sidebarBtn){sidebarBtn.addEventListener("click",function(){elementToggleFunc(sidebar);});}
const testimonialsItem=document.querySelectorAll("[data-testimonials-item]");const modalContainer=document.querySelector("[data-modal-container]");const modalCloseBtn=document.querySelector("[data-modal-close-btn]");const overlay=document.querySelector("[data-overlay]");if(modalContainer&&modalCloseBtn&&overlay){const modalImg=document.querySelector("[data-modal-img]");const modalTitle=document.querySelector("[data-modal-title]");const modalText=document.querySelector("[data-modal-text]");const testimonialsModalFunc=function(){modalContainer.classList.toggle("active");overlay.classList.toggle("active");}
testimonialsItem.forEach(item=>{item.addEventListener("click",function(){modalImg.src=this.querySelector("[data-testimonials-avatar]").src;modalImg.alt=this.querySelector("[data-testimonials-avatar]").alt;modalTitle.innerHTML=this.querySelector("[data-testimonials-title]").innerHTML;modalText.innerHTML=this.querySelector("[data-testimonials-text]").innerHTML;testimonialsModalFunc();});});modalCloseBtn.addEventListener("click",testimonialsModalFunc);overlay.addEventListener("click",testimonialsModalFunc);}
const initTypewriter=()=>{const titleElement=document.querySelector('.info-content .title');if(!titleElement)return;const roles=["Data Scientist","Python Developer","Problem Solver","Analyst"];let roleIndex=0;let charIndex=0;let isDeleting=false;let typeSpeed=100;titleElement.classList.add('typewriter-cursor');function type(){const currentRole=roles[roleIndex];if(isDeleting){titleElement.textContent=currentRole.substring(0,charIndex-1);charIndex--;typeSpeed=50;}else{titleElement.textContent=currentRole.substring(0,charIndex+1);charIndex++;typeSpeed=150;}
if(!isDeleting&&charIndex===currentRole.length){isDeleting=true;typeSpeed=2000;}else if(isDeleting&&charIndex===0){isDeleting=false;roleIndex=(roleIndex+1)%roles.length;typeSpeed=500;}
setTimeout(type,typeSpeed);}
type();};const initTiltEffect=()=>{const cards=document.querySelectorAll('.project-item, .certification-item, .events-post-item');cards.forEach(card=>{card.classList.add('tilt-effect');card.addEventListener('mousemove',(e)=>{const rect=card.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;const centerX=rect.width/2;const centerY=rect.height/2;const rotateX=((y-centerY)/centerY)*-5;const rotateY=((x-centerX)/centerX)*5;card.style.transform=`perspective(1000px)rotateX(${rotateX}deg)rotateY(${rotateY}deg)scale3d(1.02,1.02,1.02)`;});card.addEventListener('mouseleave',()=>{card.style.transform='perspective(1000px) rotateX(0) rotateY(0) scale3d(1, 1, 1)';});});};document.addEventListener('DOMContentLoaded',()=>{initTypewriter();const showSkeleton=(elementId,count=3,height='100px')=>{const container=document.getElementById(elementId);if(!container)return;container.innerHTML='';for(let i=0;i<count;i++){const div=document.createElement('div');div.classList.add('skeleton');div.style.height=height;div.style.marginBottom='20px';container.appendChild(div);}}
const escapeHTML=(str)=>{if(!str)return'';return String(str).replace(/[&<>'"]/g,tag=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[tag]||tag));};const safeUrl=(url)=>{try{const parsed=new URL(url,window.location.origin);if(parsed.protocol==='http:'||parsed.protocol==='https:')return parsed.href;}catch(e){}
return'#';};const timeAgo=(dateString)=>{if(!dateString)return'';const diff=(new Date()-new Date(dateString))/1000;if(diff<60)return'just now';const units=[['year',31536000],['month',2592000],['day',86400],['hour',3600],['minute',60]];const[unit,secs]=units.find(([,s])=>diff>=s);return new Intl.RelativeTimeFormat('en',{numeric:'auto'}).format(-Math.floor(diff/secs),unit);};const populateProjects=async()=>{const projectList=document.getElementById("project-list");if(!projectList)return;showSkeleton('project-list',4,'150px');try{const[projectsResponse,updatesResponse]=await Promise.all([fetch("/assets/data/projects.json"),fetch("/assets/data/last_updated.json")]);if(!projectsResponse.ok){throw new Error(`Failed to load projects.json:${projectsResponse.statusText}`);}
const projects=await projectsResponse.json();const updates=updatesResponse.ok?await updatesResponse.json():{};const projectsWithUpdates=projects.map(project=>{const lastUpdated=project.github?updates[project.github]:null;return{...project,updated_at:lastUpdated};});projectsWithUpdates.sort((a,b)=>{if(a.updated_at&&b.updated_at){return new Date(b.updated_at)-new Date(a.updated_at);}
if(a.updated_at)return-1;if(b.updated_at)return 1;return 0;});projectList.innerHTML='';projectsWithUpdates.forEach((project,index)=>{const li=document.createElement("li");li.className=`project-item active fade-in-up ${index===0?'featured':''}`;li.style.animationDelay=`${index*0.1}s`;li.setAttribute("data-filter-item","");li.setAttribute("data-category",project.category.toLowerCase());const tagsHtml=project.tags.map((tag)=>`<span class="tag">${tag}</span>`).join("");const updatedHtml=project.updated_at?'<p class="project-category">Last updated: '+timeAgo(project.updated_at)+'</p>':"";const isLight=document.body.getAttribute("data-theme")==="light";const lightImage=project.image.replace('.webp','_light.webp');const currentImage=isLight?lightImage:project.image;li.innerHTML=`<a href="${project.url}"target="_blank"rel="noopener noreferrer"style="display: block; height: 100%;"><figure class="project-img"><div class="project-item-icon-box"><ion-icon name="eye-outline"></ion-icon></div><img src="${currentImage}"data-dark-src="${project.image}"data-light-src="${lightImage}"class="theme-aware-img"alt="${project.alt}"loading="lazy"></figure><div class="project-info"><h3 class="project-title">${project.title}</h3><p class="project-category">${project.category_desc}</p>${updatedHtml}<div class="project-tags">${tagsHtml}</div></div></a>`;projectList.appendChild(li);});const counts={all:projectsWithUpdates.length};projectsWithUpdates.forEach(project=>{const cat=project.category.toLowerCase();counts[cat]=(counts[cat]||0)+1;});const filterBtns=document.querySelectorAll("[data-filter-btn]");filterBtns.forEach(btn=>{let baseText=btn.innerText.replace(/\s*\(\d+\)$/,'').trim();const cat=baseText.toLowerCase();if(counts[cat]!==undefined){btn.innerHTML=`${baseText}<span class="count-pill">${counts[cat]}</span>`;}});const selectItems=document.querySelectorAll("[data-select-item]");selectItems.forEach(item=>{let baseText=item.innerText.replace(/\s*\(\d+\)$/,'').trim();const cat=baseText.toLowerCase();if(counts[cat]!==undefined){item.innerHTML=`${baseText}<span class="count-pill">${counts[cat]}</span>`;}});initializeProjectFilter();setTimeout(initTiltEffect,500);}catch(error){console.error("Error loading or processing projects:",error);projectList.innerHTML='<li><p>Could not load projects. Please try again later.</p></li>';}};const initializeProjectFilter=()=>{const select=document.querySelector("[data-select]");const selectItems=document.querySelectorAll("[data-select-item]");const selectValue=document.querySelector("[data-selecct-value]");const filterBtns=document.querySelectorAll("[data-filter-btn]");const filterItems=document.querySelectorAll("[data-filter-item]");const filterFunc=function(selectedValue){for(let i=0;i<filterItems.length;i++){if(selectedValue==="all"||selectedValue===filterItems[i].dataset.category){filterItems[i].classList.add("active");}else{filterItems[i].classList.remove("active");}}}
let lastClickedBtn=filterBtns.length>0?filterBtns[0]:null;if(lastClickedBtn){for(let i=0;i<filterBtns.length;i++){filterBtns[i].addEventListener("click",function(){let selectedValue=this.childNodes[0].nodeValue.trim().toLowerCase();if(selectValue)selectValue.innerHTML=this.innerHTML;filterFunc(selectedValue);lastClickedBtn.classList.remove("active");this.classList.add("active");lastClickedBtn=this;});}}
if(select){select.addEventListener("click",function(){elementToggleFunc(this);});for(let i=0;i<selectItems.length;i++){selectItems[i].addEventListener("click",function(){let selectedCat=this.childNodes[0].nodeValue.trim().toLowerCase();if(selectValue){selectValue.innerHTML=this.innerHTML;}
elementToggleFunc(select);filterFunc(selectedCat);});}}};populateProjects();const yearElement=document.getElementById('current-year');if(yearElement){yearElement.textContent=new Date().getFullYear();}
const themeBtns=document.querySelectorAll("[data-theme-btn]");if(themeBtns.length>0){const applyTheme=(theme)=>{const isLight=theme==="light";if(isLight){document.body.setAttribute("data-theme","light");}else{document.body.removeAttribute("data-theme");}
localStorage.setItem("theme",theme);themeBtns.forEach(btn=>btn.setAttribute("aria-pressed",isLight));document.querySelectorAll('.theme-aware-img').forEach(img=>{if(isLight&&img.hasAttribute('data-light-src')){img.src=img.getAttribute('data-light-src');}else if(!isLight&&img.hasAttribute('data-dark-src')){img.src=img.getAttribute('data-dark-src');}});};const currentTheme=localStorage.getItem("theme")||"dark";applyTheme(currentTheme);themeBtns.forEach(btn=>{btn.addEventListener("click",function(){const isLight=document.body.getAttribute("data-theme")==="light";applyTheme(isLight?"dark":"light");});});}
document.addEventListener("keydown",function(e){const tag=e.target.tagName;const isEditable=e.target.isContentEditable;if(tag==="INPUT"||tag==="TEXTAREA"||tag==="SELECT"||tag==="BUTTON"||tag==="A"||isEditable)return;if((e.key==="m"||e.key==="M")&&!e.ctrlKey&&!e.metaKey){const themeBtn=document.querySelector("[data-theme-btn]");if(themeBtn)themeBtn.click();}
if(!e.ctrlKey&&!e.metaKey&&!e.altKey){const navLinks=["/","/projects/","/certifications/","/events/","/blogs/","/contact/"];const num=parseInt(e.key);if(num>=1&&num<=6){window.location.href=navLinks[num-1];}}});(async function(){var cacheKey="latest_github_activity";var cacheExpiry=60*60*1000;function renderRepoCard(repo,commit){var el=document.getElementById('cwo-content');if(!el)return;var repoUrl=safeUrl(repo&&repo.html_url);var repoName=escapeHTML(repo&&repo.name?repo.name:"Unknown Repo");var safeCommitMsg=(commit&&commit.commit&&commit.commit.message)?commit.commit.message.split('\n')[0]:"No commit message available";var commitMsg=escapeHTML(safeCommitMsg);var commitUrl=safeUrl(commit&&commit.html_url);var safeCommitDate=(commit&&commit.commit&&commit.commit.author&&commit.commit.author.date)?new Date(commit.commit.author.date).toLocaleDateString(undefined,{year:'numeric',month:'short',day:'numeric'}):"Unknown date";var commitDate=escapeHTML(safeCommitDate);el.innerHTML=`<div style="background: var(--eerie-black-2); border: 1px solid var(--jet); border-radius: 14px; padding: 20px; box-shadow: var(--shadow-2); transition: var(--transition-1);"><div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; flex-wrap: wrap; gap: 10px;"><h4 class="h4"style="margin: 0;"><a href="${repoUrl}"target="_blank"rel="noopener noreferrer"style="color: var(--white-2); text-decoration: none; transition: var(--transition-1);">${repoName}</a></h4><time style="color: var(--light-gray-70); font-size: var(--fs-6); display: flex; align-items: center; gap: 5px;"><ion-icon name="time-outline"></ion-icon>${commitDate}</time></div><p class="timeline-text"style="margin: 0; padding: 0;"><a href="${commitUrl}"target="_blank"rel="noopener noreferrer"style="color: var(--light-gray); text-decoration: none; display: flex; align-items: flex-start; gap: 8px;"><ion-icon name="git-commit-outline"style="margin-top: 3px; color: var(--vibrant-green); flex-shrink: 0;"></ion-icon><span style="transition: var(--transition-1);">${commitMsg}</span></a></p></div>`;}
try{var cached=localStorage.getItem(cacheKey);if(cached){var parsed=JSON.parse(cached);if(new Date().getTime()-parsed.timestamp<cacheExpiry){renderRepoCard(parsed.repo,parsed.commit);return;}}}catch(e){console.warn("Failed to read cache",e);}
try{var r=await fetch('https://api.github.com/users/Asifdotexe/repos?sort=pushed&per_page=1');if(!r.ok)throw new Error('repos API error');var repos=await r.json();if(!repos||repos.length===0)throw new Error('no repos');var repo=repos[0];var branch=repo.default_branch||'main';var c=await fetch('https://api.github.com/repos/Asifdotexe/'+repo.name+'/commits/'+branch);if(!c.ok)throw new Error('commits API error');var commit=await c.json();var msg=commit.commit.message;if(msg.indexOf('Merge ')===0){var cl=await fetch('https://api.github.com/repos/Asifdotexe/'+repo.name+'/commits?sha='+branch+'&per_page=5');if(cl.ok){var list=await cl.json();for(var i=0;i<list.length;i++){if(list[i].commit.message.indexOf('Merge ')!==0){var cr=await fetch('https://api.github.com/repos/Asifdotexe/'+repo.name+'/commits/'+list[i].sha);if(cr.ok){commit=await cr.json();break;}}}}}
try{localStorage.setItem(cacheKey,JSON.stringify({timestamp:new Date().getTime(),repo:repo,commit:commit}));}catch(e){console.warn("Failed to write to cache",e);}
renderRepoCard(repo,commit);}catch(e){console.warn("GitHub API rate limit reached or error occurred. Showing fallback.",e);var el=document.getElementById('cwo-content');if(el){el.innerHTML=`<div style="background: var(--eerie-black-2); border: 1px solid var(--jet); border-radius: 14px; padding: 20px; box-shadow: var(--shadow-2);"><div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px;"><h4 class="h4"style="margin: 0; color: var(--white-2);">GitHub Activity</h4></div><p class="timeline-text"style="margin: 0; color: var(--light-gray-70);">API rate limit exceeded.Please check back later.</p></div>`;}}})();});